# Core MCP and FastMCP
fastmcp>=0.5.0
mcp>=1.0.0

# Database
psycopg[binary,pool]>=3.1.0

# OAuth and Authentication (for OAuth companion service)
authlib>=1.3.0
requests>=2.31.0
python-jose[cryptography]>=3.3.0
itsdangerous>=2.1.2

# Web framework (for HTTP transport and OAuth service)
uvicorn>=0.24.0
fastapi>=0.104.0

# Data validation
pydantic>=2.5.0

# Additional utilities
python-multipart>=0.0.6

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0